{
  "name": "11ty-tuto",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run scss:watch\"  \"npx @11ty/eleventy --input=./src --serve\" ",
    "test": "echo \"Error: no test specified\" && exit 1",
    "scss:watch": "npm run scss:compile -- --watch",
    "prescss:watch": "npm run scss:compile",
    "scss:compile": "node-sass ./src/styles/main.scss -o ./src/css --style=compressed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.9.0",
    "concurrently": "^5.0.0",
    "node-sass": "^4.13.0"
  }
}
